#include <stdio.h>

int main(){
    printf("Input\n");
    float a,b,i,j,x,y;
    printf("");
    scanf("%f" "%f" "%f" "%f" "%f" "%f" , &a, &b, &i, &j, &x, &y);
    
    float hasil = (a-b)*(i/j)-(x+y);
    
    printf("\nOutput\n");
    printf("%.3f\n", hasil);

    return 0;
}